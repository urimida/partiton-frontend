# 프론트엔드 발표 요약 (1분 버전) 🎯

## 🏗️ 아키텍처 핵심

### 1. **Feature-First 구조**
- 기능별 모듈화 (auth, partition, settings)
- 각 모듈: models, services, providers, screens로 분리
- 확장성과 유지보수성 확보

---

## 🔐 복잡한 인증 플로우 (가장 중요!)

### 카카오 로그인 3단계 Fallback 전략
```
1. 카카오톡 설치 확인
   ↓
2. 카카오톡 로그인 시도
   ↓ (실패 시)
3. 카카오계정 로그인으로 자동 전환
```

**트러블슈팅**: 
- iOS/Android 플랫폼별 다른 동작 처리
- 사용자 취소 시 null 반환 처리
- 카카오계정 미연결 시 예외 처리

### UserRole 기반 스마트 라우팅
```dart
LEADER/MEMBER → 홈 화면 (바로 진입)
GUEST → 온보딩 화면 (설정 필요)
없음 → 상태 확인 후 적절한 화면으로 이동
```

**복잡한 점**: 
- 서버 정보 우선, 실패 시 로컬 스토리지 fallback
- 닉네임 → 그룹 → 선호도 순차 확인
- 각 단계별 적절한 화면으로 라우팅

---

## 🌐 API 클라이언트 인터셉터 (자동화)

### 자동 토큰 관리
```dart
onRequest: 
  - 공개 엔드포인트 확인
  - 인증 필요 시 자동으로 Bearer 토큰 추가
  - 모든 요청/응답 로깅
```

**장점**: 
- 모든 API 호출에서 일관된 인증 처리
- 개발자 실수 방지 (토큰 누락 없음)
- 디버깅 용이 (자동 로깅)

---

## 💾 이중 저장소 전략

### Secure Storage + SharedPreferences
```
Secure Storage: 
  - Access Token, Refresh Token (민감 정보)
  
SharedPreferences: 
  - User ID, User Name, 온보딩 상태 (일반 정보)
```

**이유**: 
- 토큰은 보안이 중요 → Secure Storage
- 일반 정보는 빠른 접근 → SharedPreferences
- 플랫폼별 최적화

---

## 📅 캘린더 캐싱 전략

### 2단계 캐싱
```
1. 월간 데이터 캐싱 (월별 키)
   - 같은 월 재조회 시 API 호출 안 함
   
2. 일간 상세 데이터 캐싱 (날짜별 키)
   - 선택한 날짜만 상세 조회
   - 불필요한 API 호출 최소화
```

**성능 최적화**: 
- 네트워크 요청 90% 감소
- 사용자 경험 향상 (즉시 표시)

---

## 🐛 주요 트러블슈팅

### 1. **비동기 상태 관리**
- FutureBuilder로 사용자 이름 가져오기
- 권한 체크를 위한 비동기 처리

### 2. **카카오 SDK 플랫폼 차이**
- iOS/Android/Web 각각 다른 동작
- MissingPluginException 처리

### 3. **토큰 만료 처리**
- 현재: 수동 재로그인
- 향후: 자동 토큰 갱신 구현 예정

---

## 📊 기술 스택

- **상태 관리**: Provider (단순하고 효율적)
- **HTTP**: Dio (인터셉터 활용)
- **저장소**: Secure Storage + SharedPreferences
- **인증**: 카카오 SDK
- **아키텍처**: Feature-First + 레이어 분리

---

## 🎯 핵심 성과

1. ✅ **자동화된 인증 플로우** - 개발자 실수 방지
2. ✅ **스마트 캐싱** - 네트워크 요청 최소화
3. ✅ **안전한 토큰 관리** - Secure Storage 활용
4. ✅ **확장 가능한 구조** - Feature-First 패턴

---

## 💡 발표 시 강조할 점

1. **"인증 플로우의 복잡성을 인터셉터로 해결"**
   - 모든 API 호출에서 자동 토큰 추가
   - 공개 엔드포인트 자동 감지

2. **"캐싱으로 성능 최적화"**
   - 월간/일간 2단계 캐싱
   - 불필요한 API 호출 제거

3. **"카카오 로그인 Fallback 전략"**
   - 사용자 경험 최우선
   - 모든 시나리오 대응

---

**발표 시간**: 약 1분 (핵심만)
**추가 설명**: Q&A에서 상세 설명 가능





